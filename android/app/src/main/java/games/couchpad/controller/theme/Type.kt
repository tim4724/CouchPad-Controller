package games.couchpad.controller.theme

import androidx.compose.material3.Typography

// Stock Material 3 type scale — no custom display face. The brand voice lives in
// the app icon and the games' own art, not the launcher chrome.
val Typography = Typography()
